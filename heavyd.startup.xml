<?xml version="1.0" encoding="UTF-8" ?>

<project name="HeavyD - platform" default="info">

    <target name="info" description="Show some basic information.">
        <echo message="Welcome to the HeavyD project environment." />
    </target>

    <target name="heavyd:project-retrofit" description="Adds some basics to a pre-existing project">
        <phingcall target="startup:basic-dirs"/>
    </target>

    <target name="heavyd:project-start-drupal-8" description="sets up a heavyD drupal 8 project for the very first time. ">
        <phingcall target="startup:basic-dirs"/>
    </target>

    <target name="startup:basic-dirs" description="Set up the basic directories for a heavyD project">
        <mkdir dir="${project.basedir}/properties" />
        <mkdir dir="${project.basedir}/etc" />
        <mkdir dir="${project.basedir}/script" />
        <mkdir dir="${project.basedir}/test" />
        <mkdir dir="${project.basedir}/artifact" />
    </target>
</project>
